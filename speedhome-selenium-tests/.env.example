# SpeedHome Selenium Test Environment Configuration
# Copy this file to .env and update with your actual values

# Application URLs
BASE_URL=http://localhost:5174
API_BASE_URL=http://localhost:5001

# Test User Credentials
# Create these users in your application before running tests
TENANT_EMAIL=tenant@test.com
TENANT_PASSWORD=testpass123
LANDLORD_EMAIL=landlord@test.com
LANDLORD_PASSWORD=testpass123

# Test Configuration
DEFAULT_TIMEOUT=10
BROWSER=chrome
HEADLESS=false

# Optional: Database Configuration (if tests need direct DB access)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=speedhome_test
# DB_USER=test_user
# DB_PASSWORD=test_password

# Optional: Email Testing (if testing email notifications)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=test@gmail.com
# EMAIL_PASSWORD=app_password

# Optional: File Upload Testing
# UPLOAD_DIR=/tmp/test_uploads
# MAX_FILE_SIZE=10MB

# Optional: Performance Testing
# PERFORMANCE_THRESHOLD_MS=3000
# LOAD_TEST_USERS=10

# Optional: CI/CD Configuration
# CI_BROWSER=chrome
# CI_HEADLESS=true
# CI_PARALLEL=true
# CI_MAX_FAILURES=5

